/*
TODO: Now we are testing the modulus operator in javaScript.Increment and decrement operators are used to increase or decrease the value of a variable by 1, respectively. The increment operator (++) adds 1 to the variable, while the decrement operator (--) subtracts 1 from the variable. These operators can be used in both prefix and postfix forms, which determine when the increment or decrement takes place in relation to the variable's current value.
*/

// Using increment operator
var x = 5;
x++;
console.log("After incrementing, x is: " + x); // Output: 6

// Using decrement operator
var y = 10;
y--;
console.log("After decrementing, y is: " + y); // Output: 9